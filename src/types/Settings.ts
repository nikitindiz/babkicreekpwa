export interface Settings {
  id: number;
  profileId: number;
  currency: string; // 'GEL'
  language: string; // 'en'
  timezone: string; // 'Asia/Tbilisi'
  createdAt: string;
  updatedAt: string;
}
